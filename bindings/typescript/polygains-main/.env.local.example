# Local Development Environment (TCP connection)
# Copy this to .env for local development

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
POSTGRES_PORT=5469

# TCP connection (default for macOS/Windows)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5469/postgres

# Unix socket path (preferred)
DB_SOCKET_PATH=/home/franz/poly-insider-live/v3/polygains/.pgsocket

# API Server
API_HOST=127.0.0.1
API_PORT=4069

# Frontend proxy server
FRONTEND_HOST=127.0.0.1
FRONTEND_PORT=4033
API_UPSTREAM_BASE_URL=http://127.0.0.1:4069

# Frontend API base URL (Bun auto-inlines BUN_PUBLIC_* to browser)
BUN_PUBLIC_API_BASE_URL=http://127.0.0.1:4069

# Workers / runtime
FETCH_INTERVAL_MS=3600000
NODE_ENV=development
